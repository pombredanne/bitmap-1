This is a toy packed-bitmap data structure, written for my own practice.

The numpy boolean type is "numpy.bool8", i.e., as in C++, an 8-bit integer.

We can do better, at the expense of a constant speed factor, by instead packing each boolean into a single bit.

